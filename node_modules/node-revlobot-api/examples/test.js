'use strict';
const config = require('../config.json');
const revlo = require('../lib/index')(config.key);

revlo.post.bonus('dbkynd', {
  amount: 100,
}).then(data => {
  console.log(data);
  // Do stuff
}, console.error);
